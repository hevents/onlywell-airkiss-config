# Onlywell 表芯WIFI · Airkiss 自助配网工具 使用说明

> 适用设备：Onlywell 表芯WIFI 石英钟（Espressif/乐鑫 ESP 模组，主机名形如 `ESP_XXXXXX`，具体 MAC 见各人设备）
> 适用故障：微信解绑 / 换路由后，表芯连不上 WiFi、微信扫码卡"连接中"
> 工具：`airkiss_sender.py`（纯 Python 标准库，零第三方依赖）
> 验证日期：2026-08-31（实机配网成功，表芯拿到 192.168.0.x 并正常校时）

---

## 一、为什么需要这个工具

Onlywell 表芯是**微信生态的 Airkiss 设备**：配网时手机把 WiFi 密码编码进 802.11 广播帧的"长度字段"打出去，表芯在混杂监听模式下 sniff 并解出密码联网。

问题是微信自带的配网流程在解绑 / 票据失效后**不再真正发起射频广播**（界面一直转圈，但网卡根本没发包）。而 EspTouch 这类通用 APP 发的是 Espressif **SmartConfig** 协议，表芯固件解不出，也会失败。

本工具直接把 SSID+密码按**标准 Airkiss 协议**编码成 UDP 广播帧长度序列发出，**完全不依赖微信服务器**，从而绕过失效的微信配网流程，让表芯重新联网走时。

---

## 二、环境要求

| 项目 | 要求 |
|---|---|
| 系统 | Windows（脚本自动探测 `WLAN` 网卡；Linux/macOS 改用 `0.0.0.0` 或手动 `--bind-ip` 即可） |
| Python | 3.6+，**仅需标准库**，无需 `pip install` 任何包 |
| 网络 | **PC 的无线网卡必须连上 2.4G WiFi**（与表芯要连的路由同一台）；Airkiss 不支持 5G |
| 表芯 | 处于配网监听态（见下） |

---

## 三、操作步骤（关键：监听态时机）

1. **PC 连上 2.4G WiFi**（同一台路由，与表芯目标网络一致）。
2. **表芯进监听态**：长按设备 WIFI 键 **3 秒** → 秒针**走到 12 点并停住不动**。
   - ⚠️ 这是最关键的坑：监听态**有时限**，超时（约数十秒）会自动退回走针态。
   - **必须趁秒针停在 12 不动的窗口内立刻发广播**，否则表芯 sniff 不到空中帧、必然失败。
   - 走针 ≠ 联网：秒针走时是本地 RTC 驱动，与 WiFi 无关。
3. **运行脚本**（下方命令），发完去路由器看结果。
4. **验证成功**：路由器 DHCP 客户端列表出现 `ESP_XXXXXX`（Espressif 模组）设备并拿到 IP = 配网成功。

### 命令

```bat
python airkiss_sender.py --ssid 你的WiFi名 --password 你的WiFi密码
```

示例（本机实测）：

```bat
python airkiss_sender.py --ssid <你的WiFi名称> --password <你的WiFi密码>
```

可选参数：

| 参数 | 说明 | 默认 |
|---|---|---|
| `--ssid` | WiFi 名称（必填，无默认） | — |
| `--password` | WiFi 密码（必填） | — |
| `--bind-ip` | 手动指定发出广播的无线网卡 IP（Windows 自动探测 `WLAN` 失败时用） | 自动 |
| `--port` | 目标 UDP 端口 | `10000` |
| `--dry-run` | 只编码不发包，用于自检 | 关 |

自检（不发真实包，确认编码无误）：

```bat
python airkiss_sender.py --ssid <你的WiFi名称> --password testpass --dry-run
```

---

## 四、成功判据与现象

- 脚本输出 `编码完成：共 4175 个帧` + 从 WLAN IP 广播到 `255.255.255.255:10000`，耗时约 22 秒。
- **脚本内置 ACK 监听**：发广播的同时在 `:10000` 监听设备回播，收到 random 回播即打印 `★ Airkiss 配网成功` 并提前结束广播（Onlywell 表芯配网成功必回播 `random(1B)+MAC(6B)`，详见第七、八节）。
- **最终铁证是路由器 DHCP 里出现 `ESP_XXXXXX` 这类 Espressif 设备并拿到 IP**。若本机未收到回播（被防火墙/端口占用挡掉），只要路由里出现该设备即成功。
- 表芯通常禁 ICMP/ARP 响应，`ping` 它显示"无法访问目标主机"属正常，不影响结论。

---

## 五、排错

| 现象 | 原因 | 处理 |
|---|---|---|
| `无法自动探测 WLAN 网卡 IP` | 网卡别名不是 `WLAN` 或未连 WiFi | 先连 2.4G；或用 `Get-NetIPAddress -InterfaceAlias 'WLAN' -AddressFamily IPv4` 查 IP 后加 `--bind-ip` |
| 广播发了，路由无 `ESP_XXXXXX` | **监听态已超时退回走针**（最常见） | 重按 WIFI 键 3 秒，确认秒针**停**在 12，立刻重跑脚本 |
| 用了 EspTouch 配网失败 | 它发的是 SmartConfig，表芯解不出 | 别用 EspTouch，用本工具（Airkiss 协议） |
| 微信扫码卡"连接中" | 微信后端不下发广播参数 | 放弃微信配网，用本工具广播；绑定见第六节 |

---

## 六、配网后：微信重新绑定（恢复电量查看/校时）

配网（让设备联网）与微信绑定（账号关联）是两步，互相解耦：

1. 先用本工具让表芯联网（见上）。
2. 微信里**重扫设备二维码** → 弹出的配网页里选 **「跳过联网」** → 即可完成绑定。
3. 绑定后微信内可查看**电池电量**、**同步时间**，恢复远程能力。

> 说明：之前"onlywell 云已死"是误判。本次实测微信扫码选「跳过联网」即成功绑定、可看电量/校时，证明厂家云仍存活；真正的故障点是"解绑后微信 Airkiss 下发环节失效 + 二维码票据在浏览器直开时显示失效"，而非服务端全停。

---

## 七、原理简述（供参考）

1. 设备长按 WIFI 键 3 秒进混杂监听，sniff 空中 802.11 帧。
2. 发送端把 `密码 + random + SSID` 按 Airkiss 协议编码成 9bit 值序列，每个值当作一个 **UDP 广播包（目的 `255.255.255.255:10000`）的负载字节数**打出；接收端用 `帧长度 - 基准长度` 还原编码值并解出 SSID/密码。
3. 设备联网后向 `:10000` 回播 random 字节确认（脚本会监听，但即使没收到也不影响判断，以路由器 DHCP 为准）。

编码结构（每帧 9bit 值）：前导 `[1,2,3,4]×50` → magicCode（总长+SSID的CRC8）×20 → prefixCode（密码长+其CRC8）×15 → sequence（每 4 字节一段，含序列头+数据帧）；整体外层再重复 5 次提高可靠性。已逐行对齐开源参考 `zhchbin/WeChatAirKiss`，dry-run 与实跑均验证通过。

---

## 八、附：Android APK 版（手机直发，免 PC）

不想开电脑时，可用手机直接广播 Airkiss：

- **文件**：`OnlywellAirkiss-Config-v1.2.apk`（已用 Android build-tools 34 自签名，v1/v2/v3 签名齐全，Android 5.0+ 均可安装）。当前版本 **v1.2**（下拉刷新完整动画：拖拽跟手展开、松手回弹触发、转圈「正在刷新…」后收起；切网回到应用仍自动刷新。其他功能：① 界面标题显示版本号；② 端口 10000 被占用时给出明确引导——多为微信等别的配网应用占用，广播不受影响；③ 密码框下方提示：部分表芯固件密码仅支持 8 位，超出 8 位会失败）。
- **隐私**：本工具已**去除所有具体设备的 MAC / IP / WiFi 名 / 密码**，提示语只说"看 ESP_ 开头的设备"，可放心分享给他人使用。
- **安装**：把 apk 传到手机 → 允许"安装未知应用"（设置→安全/应用安装权限）→ 点开安装。
- **权限**：`INTERNET` / `ACCESS_WIFI_STATE` / `ACCESS_FINE_LOCATION` / `NEARBY_WIFI_DEVICES`。
  - 其中 `ACCESS_FINE_LOCATION`（位置）是 **Android 10+ 读取当前 WiFi 名称(SSID)所必需**，安装后首次打开会弹窗请求，请点"允许（仅使用期间）"；若拒绝，SSID 不会自动填入，需手动输入。
- **核心能力**：
  1. **自动填入当前 SSID**：打开 App 即读取手机所连 WiFi 名称并填进输入框。
  2. **2.4G / 5G 检测与警告**：读信道频率；5G 时红色大字警告并拦截发送；2.4G 时绿色提示"当前 WiFi：xxx（2.4G yyyyMHz）"。
  3. **广播进度条**：随帧发送实时推进，过程可见。
  4. **ACK 回播监听（成功判据）**：广播同时在 `:10000` 监听设备回播，按 `random(1B)+MAC(6B)` 解析；收到即**立即**把状态栏置绿"✅ 已收到设备回播，设备 MAC=…"（即时反馈，不干等）；MAC 与路由器 DHCP 的 `ESP_` 设备一致即成功。监听窗口覆盖"广播总时长 + 10 秒"，避免早期 6 秒窗口漏接。
  5. **重发清屏**：每次重发前清空上一轮日志，界面永远干净独立。
  6. **失败友好提示**：未收到回播时提示"请重新发送配网广播试试（确认表芯处于监听态）"——因为 Onlywell 成功必回播，未收到多为表芯未处于监听态。
- **使用**：打开 App（图标"Onlywell配网"）→ 确认 SSID 已自动填好、频段提示为绿色 2.4G → 补上密码 → 点「发送配网广播」→ **同时确认表芯处于监听态（秒针停在 12 不动）** → 看状态栏绿字 / 去路由器看 `ESP_XXXXXX` 是否上线。
- **与 PC 版完全等价**：同一套 Airkiss 编码（移植进 `airkiss_app/src/com/onlywell/airkiss/AirKissEncoder.java`），同样目的端口 `10000`、帧间隔 4ms。
- **源码**：`airkiss_app/`（Java 源 + `res/` + `AndroidManifest.xml` + `build_apk.sh`）。要改界面或重编译，装好 Android SDK（含 build-tools 34、platforms;android-34）后运行 `bash build_apk.sh` 即可重新出包。
- ⚠️ 手机必须连 **2.4G WiFi**；5G 下 Airkiss 设备 sniff 不到帧（App 会主动拦截并提示）。

### 版本演进记录

| 版本 | 关键变更 |
|---|---|
| **v1.2** | 下拉刷新完整动画：拖拽跟手展开+阻尼，拉过阈值提示「松开立即刷新」，**松手回弹触发**（转圈「正在刷新…」后平滑收起）；未达阈值收起不刷新 |
| **v1.1** | **WiFi 刷新**：切网回到应用自动刷新当前 WiFi（SSID+频段）；页面上部下拉手动刷新；手输过 SSID 不被覆盖 |
| **v1.0** | **首个正式版（2026-09-02 发布）**，功能为下列内测版本之合集 |
| v1.3 | 初版；ACK 监听（按 IP 解析）+ ARP 扫描兜底 |
| v1.4 | 诊断版：ACK 监听窗口从固定 6 秒延长到「广播 + 10 秒」；ARP 加诊断 dump |
| v1.5 | 修 `EADDRINUSE`（`SO_REUSEADDR` 绑定 10000）；**砍掉 ARP 扫描**（Android EACCES 证实不可用） |
| v1.6 | ACK 按 **MAC(6B)** 解析显示设备 MAC（修正此前错显成 IP 的 bug）；打印 ACK 原始字节 |
| v1.7 | 监听计数 `loops++` 移到排除本机回环之后（计数诚实）；删硬编码"与表芯一致"、改中性核对提示 |
| v1.8 | 每次重发前清空上一轮日志 |
| v1.9 | 失败提示改为"请重新发送配网广播试试（确认表芯处于监听态）" |
| v1.10 | ACK 收到立即置绿（即时反馈）；已收到则不打"继续等待 35 秒"；清理死字段 |
| v1.11 | 收到 ACK 时同时显示回播包源 IP 地址（IP 头源地址即设备发包地址，与 MAC 一并展示；已实机验证 = 设备 LAN IP） |
| v1.12 | 密码输入框改为明文显示（textVisiblePassword），输入时可直接核对 |
| v1.13 | ① 根治端口 10000 泄漏（`finally` 关闭监听 socket），bind 失败明确提示「强行停止本应用/等约 35 秒」；② 发送侧 `255.255.255.255` 全 1 广播被拒（`EPERM`）自动切换为子网定向广播（本机 IP 末段置 255）兜底，无需重启手机 |
| v1.14 | ① 界面标题显示版本号（`Onlywell配网 vX.Y`）；② 修正 `EADDRINUSE` 引导——强制停止本应用仍占用，说明占用者是微信/安信可IoT 等别的配网应用（同样用 10000 收设备回播），引导强制停止对应应用或重启；并明确广播不受影响，仍可在路由器侧确认 `ESP_` 设备上线；③ 密码框下方新增提示：部分表芯固件密码仅支持 **8 位**；实测 8 位可成功、超过 8 位均失败（疑为设备 Airkiss 接收缓冲上限，非协议限制亦非本工具编码问题） |
