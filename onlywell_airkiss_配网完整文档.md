# Onlywell 表芯WIFI 石英钟 · Airkiss 配网与排查完整文档

> 整理日期：2026-08-31
> 适用设备：Onlywell「表芯WIFI」石英钟（Espressif/乐鑫 ESP 模组，主机名形如 `ESP_XXXXXX`，MAC OUI `5C:CF:7F`）
> 适用故障：微信解绑 / 换路由后，表芯连不上 WiFi、微信扫码卡「连接中」
> 配套工具：`airkiss_sender.py`（PC 版）+ `OnlywellAirkiss-Config-v1.x.apk`（手机版）
> 验证结论：已实机配网成功，表芯拿到 `192.168.0.x`、正常校时、微信可查电量/同步时间

---

## 一、设备与故障背景

| 项目 | 内容 |
|---|---|
| 设备 | 淘宝 onlywell 企业店购「表芯WIFI」石英钟（指针式，无 LED 屏） |
| 配网状态指示 | 长按 WIFI 键 **3 秒** → **秒针走到 12 点并停住不动** = 已进入 Airkiss 监听态（替代 LED 闪烁） |
| WiFi 模组 | Espressif ESP 类，MAC OUI `5C:CF:7F`（例：`5C:CF:7F:XX:XX:XX`，X 见各人设备） |
| 配网方式 | 微信 **Airkiss** 扫码配网（静默监听广播，不发热点） |
| 故障起点 | 微信解绑 / 换路由后，设备再也无法连上 WiFi、二维码扫出来卡在「连接中」 |

---

## 二、根因厘清（为什么微信和 EspTouch 都失败）

| 尝试 | 结果 | 根因 |
|---|---|---|
| 微信内置 Airkiss 配网 | 界面死转圈、设备始终不上线 | 微信因**解绑 / 二维码票据失效**，已不再真正发起射频广播（界面转圈时网卡 tx 全程 idle）。这是「配网失败」的真因，**不是云死** |
| 通用 EspTouch App | 「配网失败」、路由无 `ESP_` 设备 | EspTouch 发的是 Espressif **SmartConfig** 协议，Onlywell 表芯固件**只解 Airkiss**，解不出 SmartConfig |
| 本仓库 `airkiss_sender.py` / APK | **成功**，表芯拿到 IP 并校时 | 按标准 Airkiss 协议直接广播 SSID+密码，完全绕过微信服务器 |

> **结论**：onlywell 云端**仍存活**。用户在微信内重扫设备二维码 → 选「跳过联网」→ 成功绑定，现已可查电量、同步时间。所谓「云与官网停摆、用户侧无自助路径」是早期配网失败阶段的误判，已被实测推翻。

---

## 三、Airkiss 协议原理

1. 设备长按 WIFI 键 3 秒进混杂监听态，sniff 空中 802.11 帧。
2. 发送端把 `密码 + random + SSID` 按 Airkiss 协议编码成一串 **9bit 整数**；每个整数当作一个 **UDP 广播包（目的 `255.255.255.255:10000`）的负载字节数**发出。接收端用 `帧长度 − 基准长度` 还原编码值，解出 SSID/密码。
3. 设备联网后，向 `:10000` **回播一个 ACK 包**作为确认。

**编码结构（每个 9bit 值）**：
- 前导 `leadingPart`：`[1,2,3,4]` 重复 50 次（让接收端锁定信道、算出基准长度）
- `magicCode`：总长度 + SSID 的 CRC8，重复 20 次
- `prefixCode`：密码长度 + 其 CRC8，重复 15 次
- `sequence`：每 4 字节一段，含序列头（CRC8 + index）+ 数据帧
- 整体外层再重复 **5 次**提高可靠性

编码逐行对齐开源参考 `zhchbin/WeChatAirKiss`，dry-run 与实跑均验证通过。一次完整广播约 **4175 帧**（代码标称 ~16.7 秒，实测因调度开销 ~22 秒，帧间隔 4ms）。

---

## 四、设备侧 ACK 回播实测结论（核心硬知识）

> 这一段是反复迭代、用字节级证据坐实的关键结论，决定整个工具的检测逻辑。

**事实 1：Onlywell 表芯配网成功必回播 ACK。**
APK 监听到 `✅ 收到设备回播(random 匹配)`，对应 raw 字节：
```
51 5C CF 7F 2D 96 D6
│  └──────────────┘
│        MAC(6B)
random(1B)=0x51=81   ← 与本次广播的 random=81 精确命中
```
- 总长 **7 字节 = `random(1B) + MAC(6B)`**，`MAC` 之后**一个字节都没有**。

**事实 2：标准 Airkiss ACK 应为 `random(1B)+IP(4B)`，但 Onlywell 固件回的是 `random+MAC`。**
这是该设备的非标准之处：早期版本把 `buf[1..4]` 当 IP 解析，得到 `92.207.127.45` —— 那其实是 MAC 前 4 字节 `5C.CF.7F.2D`（`0x5C.0xCF.0x7F.0x2D`）。真 LAN IP 只在路由器 DHCP 侧。

**事实 3：ACK 载荷里不含 IP，只有 MAC；但回播包的 IP 头源地址就是设备 IP。**
因此 App 的成功判据是「显示设备 MAC」，`MAC` 与路由器 DHCP 里 `ESP_2D96D6 / 5C-CF-7F-2D-96-D6` 一致即可对上号。App 同时显示回播包的**源 IP 地址**（UDP 包 IP 头源地址，设备联网后才发包，该地址即其网卡所用 IP；**已实机验证**：显示值与路由 DHCP 分配给表芯的 LAN IP 一致，与安信可 IoT 小程序显示 IP 的原理相同）。

**事实 4：监听窗口必须覆盖「广播总时长 + 联网余量」。**
设备要收满约 2.5 遍数据序列才解得出密码（进度条走一半 ≈ 8~11 秒才联网），随后才回播。早期版本把监听 `setSoTimeout` 设成固定 **6 秒**（从广播开始算），第 6 秒就关窗 → 必然漏接 ACK，造成「检测不到」的假阴性。修正：窗口 = 广播开始 + `n×6ms` + 10 秒（≈32~35 秒）。

**事实 5：Android 非特权 App 读不到 `/proc/net/arp`（EACCES）。**
由此，早期「局域网 ARP 扫描兜底」这条路在你这台机器上**永久堵死**，与是否同网段无关。最终方案**移除 ARP 扫描**，仅保留 ACK 回播监听。

---

## 五、配网工具使用步骤

> 关键：广播必须在表芯**真正处于监听态（秒针停在 12 不动）**的窗口内发出。监听态超时（约数十秒）会自动退回走针态 → sniff 不到空中帧 → 必失败。

### ① 微信小程序（最简单，无需安装）

微信搜索「**一键配网**」或「**安信可 IoT**」小程序 → 选 **AirKiss 配网** → 连 **2.4G WiFi** → 输入 WiFi 密码 → 点「一键配网」→ **同时表芯长按 WIFI 键 3 秒，让秒针停在 12 点（监听态）**。

- 这两个小程序均内置标准 Airkiss 协议，与表芯完全兼容，是最快捷的配网方式。
- 手机必须连 2.4G WiFi，Airkiss 不支持 5G。

### ② 手机版（Android APK，免 PC）

安装 `OnlywellAirkiss-Config-v1.3.apk`（图标「Onlywell配网」）→ 确认 SSID 已自动填好、频段提示为绿色 2.4G → 补密码（明文显示，可直接核对；**注意：部分表芯固件密码仅支持 8 位，超过 8 位会失败**，详见下方「附」） → 点「发送配网广播」→ **同时确认表芯处于监听态（秒针停在 12 不动）**。

权限：`INTERNET` / `ACCESS_WIFI_STATE` / `ACCESS_FINE_LOCATION` / `NEARBY_WIFI_DEVICES`。其中 `ACCESS_FINE_LOCATION` 是 Android 10+ 读取 SSID 所必需，安装后首次打开请点「允许（仅使用期间）」；若拒绝则 SSID 需手动输入。手机必须连 2.4G WiFi。

### ③ PC 版（airkiss_sender.py，纯 Python 标准库）

环境：Windows（脚本自动探测 `WLAN` 网卡；Linux/macOS 用 `0.0.0.0` 或 `--bind-ip`）；Python 3.6+，零第三方依赖；PC 无线网卡连上 **2.4G WiFi**（与表芯目标路由同一台，Airkiss 不支持 5G）。

```bat
python airkiss_sender.py --ssid 你的WiFi名 --password 你的WiFi密码
```
- `--bind-ip`：手动指定发出广播的无线网卡 IP（自动探测失败时）
- `--port`：目标端口（默认 `10000`）
- `--dry-run`：只编码不发包，自检用

脚本**内置 ACK 监听**：发出广播的同时在 `:10000` 监听回播，收到 random 回播即打印 `★ Airkiss 配网成功`，并提前结束广播。

---

## 六、成功判据与实时反馈

| 判据 | 说明 |
|---|---|
| ① App 监听到 ACK | 状态栏变绿「✅ 已收到设备回播，设备 MAC=5C:CF:7F:XX:XX:XX」，且 MAC 与路由 DHCP 的 `ESP_` 设备一致 → **配网成功** |
| ② PC 脚本收到回播 | 打印 `★ Airkiss 配网成功` |
| ③ 路由器 DHCP | 出现 `ESP_XXXXXX`（Espressif 模组）设备并拿到 IP = 配网成功（最终铁证，即便本机未收到回播也以此为准） |
| ④ 表芯走秒 / 微信可查 | 配网后表芯开始走时、微信可查电量与同步时间，证明云端握手成功 |

> 表芯通常禁 ICMP/ARP 响应，`ping` 它显示「无法访问目标主机」属正常，不影响结论。

---

## 七、失败判据与规避

| 现象 | 原因 | 处理 |
|---|---|---|
| 未收到设备回播 | **表芯未处于监听态**（监听超时退回走针，最常见） | 长按 WIFI 键 3 秒确认秒针**停**在 12 → **立即**重发广播；App 已把失败提示改为「请重新发送配网广播试试（确认表芯处于监听态）」 |
| `EADDRINUSE` / 监听异常 | 上一次发送残留 socket 占住 `:10000`（卸载不清，重启才释放） | v1.5 起监听 socket 已加 `SO_REUSEADDR` 规避；如仍出现，重启手机或换端口 |
| 用了 EspTouch 失败 | 它发 SmartConfig，表芯解不出 | 改用本工具（Airkiss） |
| 微信扫码卡「连接中」 | 微信后端不下发广播参数 | 放弃微信配网，用本工具广播 |

> 第一次发送常失败、第二次才成功，几乎都是监听态时机问题，不是工具 bug。建议：**先长按表芯进监听，再立刻点发送**，间隔别超几秒。

---

## 八、配网后：微信重新绑定（恢复电量查看/校时）

配网（让设备联网）与微信绑定（账号关联）是两步，互相解耦：

1. 先用本工具让表芯联网（见第五节）。
2. 微信里**重扫设备二维码** → 弹出的配网页选 **「跳过联网」** → 完成绑定。
3. 绑定后微信内可查**电池电量**、**同步时间**，恢复远程能力。

---

## 九、常见误区澄清

- **「走针 = 联网」❌**：指针走时是本地 RTC 驱动，与 WiFi 无关；从监听态退回走针只表示「配网等待超时退出」。
- **「WiFi 列表找不到热点 = 没进配网」❌**：Airkiss 模式下设备静默监听，本就不广播热点；找不到热点恰说明进了监听态（正常）。
- **「之前 DHCP 有 ESP 设备 = 现在已联网」❌**：可能是 NVS 旧密码自动连的残留；按 WIFI 键已将其踢离，且路由重启后未回连。
- **「ACK 里能拿到设备 IP」❌**：Onlywell 固件 ACK 只回 `random+MAC`，不含 IP；IP 只能去路由器 DHCP 看。
- **「Android 能扫 ARP 表兜底」❌**：非特权 App 读 `/proc/net/arp` 被 SELinux 拒绝（EACCES），此路不可用。

---

## 十、路由器 2.4G 设置建议（提高 Airkiss 成功率）

Airkiss 广播最常被以下设置掐断，可顺手调一次：

- 2.4G 与 5G **SSID 分开命名**，手机连 2.4G 那个；
- 加密：**WPA2**（关 WPA3），模式 **802.11 b/g/n 混合**；
- 带宽固定 **20MHz**，信道固定 **1 / 6 / 11**；
- 关掉 **双频合一 / AP 隔离 / 客模式 / MAC 过滤**。

调完后：长按 WIFI 键 3 秒（秒针到 12）→ 立刻用工具广播，手机保持前台别锁屏。

---

## 附：版本演进记录（供维护参考）

| 版本 | 关键变更 |
|---|---|
| **v1.3** | **修复下拉刷新 bug**：下拉一次后顶部提示第一行（「① 手机连上 2.4G WiFi」）被顶出屏幕且不再恢复——① 刷新头部布局去掉负 margin（负 margin+动态高度在部分 ROM 测量异常）；② 拖拽激活死区 12px→28px（轻碰不再误触）；③ 松手 / 回到前台 / 收起动画结束**三处兜底强制归零**，内容必定复位 |
| **v1.2** | 下拉刷新完整动画：拖拽跟手展开+阻尼，拉过阈值提示「松开立即刷新」，**松手回弹触发**（转圈「正在刷新…」后平滑收起）；未达阈值收起不刷新 |
| **v1.1** | **WiFi 刷新**：切网回到应用自动刷新当前 WiFi（SSID+频段）；页面上部下拉手动刷新；手输过 SSID 不被覆盖 |
| **v1.0** | **首个正式版（2026-09-02 发布）**，功能为下列内测版本之合集 |
| 内测 v1.3 | 初版 APK；ACK 监听（按 IP 解析）+ ARP 扫描兜底 |
| 内测 v1.4 | 诊断版：ACK 监听窗口从固定 6 秒延长到「广播 + 10 秒」；ARP 加 `/proc/net/arp` 诊断 dump |
| 内测 v1.5 | 修 `EADDRINUSE`（`SO_REUSEADDR` 绑定 10000）；**砍掉 ARP 扫描**（EACCES 证实不可用） |
| 内测 v1.6 | ACK 按 **MAC(6B)** 解析显示设备 MAC（修正此前错显成 IP 的 bug）；打印 ACK 原始字节 |
| 内测 v1.7 | 监听计数 `loops++` 移到排除本机回环之后（计数诚实）；删硬编码「与表芯一致」、改中性核对提示 |
| 内测 v1.8 | 纯 B：每次重发前清空上一轮日志，避免多轮结果挤在一起被截断 |
| 内测 v1.9 | 失败提示从「去路由器查」改为「请重新发送配网广播试试（确认表芯处于监听态）」 |
| v1.10 | ACK 一收到**立即置绿**（即时反馈）；广播跑完后若已收到 ACK 不再打误导性的「继续等待 35 秒」；清理死字段 |
| v1.11 | 收到 ACK 时同时显示回播包**源 IP 地址**（IP 头源地址即设备发包地址，与 MAC 一并展示；已实机验证 = 设备 LAN IP） |
| v1.12 | 密码输入框改为**明文显示**（textVisiblePassword），输入时可直接核对 |
| v1.13 | ① 根治端口 10000 泄漏（`finally` 关闭监听 socket），bind 失败明确提示「强行停止本应用/等约 35 秒」；② 发送侧 `255.255.255.255` 全 1 广播被拒（`EPERM`）自动切换为**子网定向广播**（本机 IP 末段置 255）兜底，无需重启手机 |
| v1.14 | ① 界面标题显示版本号（`Onlywell配网 vX.Y`）；② 修正 `EADDRINUSE` 引导——强制停止本应用仍占用，说明占用者是微信/安信可IoT 等别的配网应用（同样用 10000 收设备回播），引导强制停止对应应用或重启；并明确广播不受影响，仍可在路由器侧确认 `ESP_` 设备上线；③ 密码框下方新增提示：部分表芯固件密码仅支持 **8 位**；实测 8 位可成功、超过 8 位均失败（疑为设备 Airkiss 接收缓冲上限，非协议限制亦非本工具编码问题，请用恰好 8 位密码） |

> 源码见 `airkiss_app/`（Java 源 + `res/` + `AndroidManifest.xml` + `build_apk.sh`）。装好 Android SDK（build-tools 34、platforms;android-34）后运行 `bash build_apk.sh` 即可重出包。
